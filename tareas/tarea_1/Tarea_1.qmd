---
title: "Tarea 1 - Estimación por Máxima Verosimilitud"
author: "Carlos Guillermo Mayorga Tapia"
id: "00294506"
course: "Modelos Lineales Generalizados (MAT5508)"
format:
  html:
    theme: cosmo
    toc: true
    number-sections: true
    math: true
  pdf:
    geometry: "margin=1in"
    number-sections: true
---

# Distribución Normal

Suponga que $Y_1, \ldots, Y_n$ son variables aleatorias normales independientes e idénticamente distribuidas con media $\mu$ y varianza conocida $\sigma^2$. La función de densidad de cada $Y_i$ está dada por:

$$
f(y; \mu) = (2\pi\sigma^2)^{-1/2} \exp\left\{ - \frac{(y-\mu)^2}{2\sigma^2} \right\}
$$

## Inciso 1: Función de Verosimilitud y Log-verosimilitud

**Supuesto:** Las observaciones $Y_1, \dots, Y_n$ son independientes e idénticamente distribuidas (i.i.d.).

Dado el supuesto de independencia, la función de verosimilitud conjunta $L(\mu)$ se obtiene multiplicando las funciones de densidad marginales:

$$
\begin{align*}
L(\mu) &= \prod_{i=1}^n f(y_i; \mu) \\
&= \prod_{i=1}^n (2\pi\sigma^2)^{-1/2} \exp\left\{ - \frac{(y_i-\mu)^2}{2\sigma^2} \right\} \\
&= (2\pi\sigma^2)^{-n/2} \exp\left\{ - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i-\mu)^2 \right\}
\end{align*}
$$

La función de log-verosimilitud $\ell(\mu)$ es el logaritmo natural de la función de verosimilitud, lo cual facilita los cálculos analíticos. Usando las propiedades de los logaritmos, obtenemos:

$$
\begin{align*}
\ell(\mu) &= \ln L(\mu) \\
&= \ln\left[ (2\pi\sigma^2)^{-n/2} \right] + \ln\left[ \exp\left\{ - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i-\mu)^2 \right\} \right] \\
&= -\frac{n}{2} \ln(2\pi\sigma^2) - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i-\mu)^2
\end{align*}
$$

---

## Inciso 2: Funciones de Puntuación e Información y Estimador MLE

### Función de puntuación (Score function)
La función de puntuación, denotada por $U(\mu)$ (o $S(\mu)$), se define como la primera derivada de la log-verosimilitud con respecto al parámetro de interés $\mu$:

$$
\begin{align*}
U(\mu) &= \frac{\partial \ell(\mu)}{\partial \mu} \\
&= \frac{\partial}{\partial \mu} \left( -\frac{n}{2} \ln(2\pi\sigma^2) - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i-\mu)^2 \right) \\
&= - \frac{1}{2\sigma^2} \sum_{i=1}^n \left[ 2(y_i-\mu)(-1) \right] \\
&= \frac{1}{\sigma^2} \sum_{i=1}^n (y_i-\mu)
\end{align*}
$$

Reordenando la suma, sabemos que $\sum_{i=1}^n (y_i-\mu) = \sum_{i=1}^n y_i - n\mu$. Como la media muestral es $\bar{y} = \frac{1}{n} \sum_{i=1}^n y_i$, tenemos que $\sum y_i = n\bar{y}$. Por lo tanto:

$$
U(\mu) = \frac{n}{\sigma^2} (\bar{y} - \mu)
$$

### Matriz (Función) de información de Fisher
La información de Fisher esperada $I(\mu)$ se define como el valor esperado negativo de la segunda derivada de la log-verosimilitud con respecto a $\mu$:

$$
I(\mu) = -E\left[ \frac{\partial^2 \ell(\mu)}{\partial \mu^2} \right]
$$

Calculamos la segunda derivada:

$$
\begin{align*}
\frac{\partial^2 \ell(\mu)}{\partial \mu^2} &= \frac{\partial}{\partial \mu} \left[ \frac{n}{\sigma^2} (\bar{y} - \mu) \right] \\
&= -\frac{n}{\sigma^2}
\end{align*}
$$

Dado que es una constante (no depende de las variables aleatorias $Y_i$), su valor esperado es la misma constante:

$$
I(\mu) = -E\left[ -\frac{n}{\sigma^2} \right] = \frac{n}{\sigma^2}
$$

### Estimador de Máxima Verosimilitud (MLE)
Para encontrar el estimador de máxima verosimilitud $\hat{\mu}$, igualamos la función de puntuación a cero y resolvemos para $\mu$:

$$
U(\hat{\mu}) = 0 \implies \frac{n}{\sigma^2} (\bar{y} - \hat{\mu}) = 0
$$

Dado que $\frac{n}{\sigma^2} \neq 0$, debe cumplirse que:
$$
\bar{y} - \hat{\mu} = 0 \implies \hat{\mu} = \bar{y}
$$

Para asegurar que se trata de un máximo global, evaluamos la segunda derivada en $\hat{\mu}$. Como $I(\mu) = \frac{n}{\sigma^2} > 0$, la segunda derivada evaluada en el estimador es $-\frac{n}{\sigma^2} < 0$. Al ser estrictamente cóncava, $\hat{\mu} = \bar{y}$ maximiza la verosimilitud.

---

## Inciso 3: Distribución Exacta y Estadístico LRS

### Distribución de la media muestral
Tenemos que $\bar{y} = \frac{1}{n} \sum_{i=1}^n y_i$. Al ser los $Y_i$ variables aleatorias normales e independientes, cualquier combinación lineal de ellas también sigue una distribución normal.
Calculamos su valor esperado y varianza:
$$
E[\bar{Y}] = \frac{1}{n} \sum_{i=1}^n E[Y_i] = \frac{1}{n} (n\mu) = \mu
$$
Por independencia:
$$
Var(\bar{Y}) = \frac{1}{n^2} \sum_{i=1}^n Var(Y_i) = \frac{1}{n^2} (n\sigma^2) = \frac{\sigma^2}{n}
$$
Por lo tanto, la distribución exacta de $\bar{Y}$ es:
$$
\bar{Y} \sim \mathcal{N}\left(\mu, \frac{\sigma^2}{n}\right)
$$

### Distribución exacta del estadístico LRS
El estadístico de razón de log-verosimilitudes (Likelihood Ratio Statistic, LRS) está dado por:
$$
-2r(\mu) = 2[\ell(\hat{\mu}) - \ell(\mu)]
$$

Sustituyendo el valor del MLE $\hat{\mu} = \bar{y}$ en la log-verosimilitud:

$$
\begin{align*}
\ell(\hat{\mu}) &= -\frac{n}{2} \ln(2\pi\sigma^2) - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i - \bar{y})^2 \\
\ell(\mu) &= -\frac{n}{2} \ln(2\pi\sigma^2) - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i - \mu)^2
\end{align*}
$$

Restando ambas ecuaciones:
$$
\ell(\hat{\mu}) - \ell(\mu) = -\frac{1}{2\sigma^2} \sum_{i=1}^n (y_i - \bar{y})^2 + \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i - \mu)^2
$$

Utilizando el teorema algebraico $\sum (y_i - \mu)^2 = \sum (y_i - \bar{y})^2 + n(\bar{y} - \mu)^2$, obtenemos:

$$
\begin{align*}
\ell(\hat{\mu}) - \ell(\mu) &= \frac{1}{2\sigma^2} \left[ \sum (y_i-\bar{y})^2 + n(\bar{y}-\mu)^2 - \sum (y_i-\bar{y})^2 \right] \\
&= \frac{1}{2\sigma^2} [n(\bar{y}-\mu)^2] = \frac{n}{2\sigma^2}(\bar{y}-\mu)^2
\end{align*}
$$

Entonces, el estadístico LRS se simplifica a:
$$
-2r(\mu) = 2 \left[ \frac{n}{2\sigma^2}(\bar{y}-\mu)^2 \right] = \frac{n}{\sigma^2}(\bar{y}-\mu)^2
$$

Reescribiéndolo en la forma de estandarización normal:
$$
-2r(\mu) = \left( \frac{\bar{y} - \mu}{\sigma/\sqrt{n}} \right)^2
$$

Sabemos por la parte anterior que $\bar{Y} \sim \mathcal{N}\left(\mu, \sigma^2/n\right)$. Al estandarizar, la variable $Z = \frac{\bar{Y} - \mu}{\sigma/\sqrt{n}}$ sigue una distribución normal estándar $Z \sim \mathcal{N}(0, 1)$.

Por definición, el cuadrado de una variable aleatoria normal estándar tiene una distribución Chi-cuadrada con $1$ grado de libertad. Concluimos que:
$$
-2r(\mu) = Z^2 \sim \chi^2_1
$$

---

## Inciso 4: Modelo de Regresión con Componentes de Covariables

**Supuesto:** Respuestas independientes pero no idénticamente distribuidas. Sea $Y_i \sim \mathcal{N}(\mu_i, \sigma^2)$ para $i=1, \dots, n$.
El predictor lineal se define como $\eta_i = \mathbf{x}_i^\top \boldsymbol{\beta}$ con un modelo de ligadura identidad $\mu_i = \eta_i = \mathbf{x}_i^\top \boldsymbol{\beta}$.
Donde:
- $\mathbf{x}_i^\top = [1, x_{i1}, \dots, x_{i,p-1}]$ (vector fila $1 \times p$)
- $\boldsymbol{\beta} = [\beta_0, \beta_1, \dots, \beta_{p-1}]^\top$ (vector columna $p \times 1$)

La función de log-verosimilitud para el vector de parámetros $\boldsymbol{\beta}$ es:
$$
\ell(\boldsymbol{\beta}) = -\frac{n}{2} \ln(2\pi\sigma^2) - \frac{1}{2\sigma^2} \sum_{i=1}^n (y_i - \mathbf{x}_i^\top \boldsymbol{\beta})^2
$$

Podemos expresar el término de suma de cuadrados utilizando notación matricial:
- $\mathbf{Y}$ es el vector columna $n \times 1$ de respuestas.
- $\mathbf{X}$ es la matriz de diseño $n \times p$ donde la $i$-ésima fila es $\mathbf{x}_i^\top$.

$$
\ell(\boldsymbol{\beta}) = -\frac{n}{2} \ln(2\pi\sigma^2) - \frac{1}{2\sigma^2} (\mathbf{Y} - \mathbf{X}\boldsymbol{\beta})^\top (\mathbf{Y} - \mathbf{X}\boldsymbol{\beta})
$$

### Vector de puntuación $S(\boldsymbol{\beta})$
El vector de puntuación de dimensión $p \times 1$ se obtiene tomando el gradiente de la log-verosimilitud con respecto al vector $\boldsymbol{\beta}$. Usando reglas de derivación matricial $\frac{\partial}{\partial \boldsymbol{\beta}} [(\mathbf{y}-\mathbf{X}\boldsymbol{\beta})^\top(\mathbf{y}-\mathbf{X}\boldsymbol{\beta})] = -2\mathbf{X}^\top(\mathbf{y}-\mathbf{X}\boldsymbol{\beta})$:

$$
\begin{align*}
S(\boldsymbol{\beta}) &= \frac{\partial \ell(\boldsymbol{\beta})}{\partial \boldsymbol{\beta}} \\
&= -\frac{1}{2\sigma^2} \left[ -2 \mathbf{X}^\top (\mathbf{Y} - \mathbf{X}\boldsymbol{\beta}) \right] \\
&= \frac{1}{\sigma^2} \mathbf{X}^\top (\mathbf{Y} - \mathbf{X}\boldsymbol{\beta})
\end{align*}
$$

O escrito en términos de sumatoria para la $j$-ésima componente:
$$
\frac{\partial \ell(\boldsymbol{\beta})}{\partial \beta_j} = \frac{1}{\sigma^2} \sum_{i=1}^n (y_i - \mathbf{x}_i^\top \boldsymbol{\beta}) x_{ij}
$$

### Matriz de información de Fisher $I(\boldsymbol{\beta})$
La matriz de información de dimensión $p \times p$ se obtiene tomando el valor esperado negativo del Hessiano de la log-verosimilitud:

$$
I(\boldsymbol{\beta}) = -E\left[ \frac{\partial^2 \ell(\boldsymbol{\beta})}{\partial \boldsymbol{\beta} \partial \boldsymbol{\beta}^\top} \right]
$$

Calculamos el Hessiano, derivando el vector de puntuación:
$$
\begin{align*}
\frac{\partial^2 \ell(\boldsymbol{\beta})}{\partial \boldsymbol{\beta} \partial \boldsymbol{\beta}^\top} &= \frac{\partial}{\partial \boldsymbol{\beta}^\top} \left[ \frac{1}{\sigma^2} \mathbf{X}^\top (\mathbf{Y} - \mathbf{X}\boldsymbol{\beta}) \right] \\
&= \frac{1}{\sigma^2} \mathbf{X}^\top (-\mathbf{X}) = -\frac{1}{\sigma^2} \mathbf{X}^\top \mathbf{X}
\end{align*}
$$

Dado que $\mathbf{X}$ es una matriz de covariables determinista (conocida y sin variables aleatorias que dependan del error), el valor esperado de la matriz constante es la misma matriz:

$$
I(\boldsymbol{\beta}) = -E\left[ -\frac{1}{\sigma^2} \mathbf{X}^\top \mathbf{X} \right] = \frac{1}{\sigma^2} \mathbf{X}^\top \mathbf{X}
$$
